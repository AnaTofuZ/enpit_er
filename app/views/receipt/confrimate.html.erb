<!--ここに取得したレシート内容の一覧を表示する。-->
<h2>以下のレシート情報が読み込まれました。</h2>
<br>
<br>
<% if !@text.nil? %>
  <%= form_for @item do |f| %>
  <table border=1 id="receipt">
    <tr><th>商品名</th><th>削除</th></tr>
    <% stext = @text %>
    <% stext.each do |st| %>
    <tr>
      <td><%=  f.text_field :food ,value: st %></td>
      <td><%= link_to "削除", receipt_confrimate_path(text: stext, dl: st), :method => :post,class: "btn btn-lg" %></td>
    </tr>
    <% end %>
  </table>
  <br><br>
  <%= f.submit "確定!!",class: "btn btn btn-primary" %>
  <% end %>
<% elsif !@message.nil? %>
  <%= @message %>
<% end %>